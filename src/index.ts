import ScormProvider from "./scormProvider";
import { ScoContext, ScormProviderProps } from "./scormProvider";
import withScorm from "./withScorm";

export default ScormProvider;
export { ScormProviderProps, ScoContext };
export { withScorm };
